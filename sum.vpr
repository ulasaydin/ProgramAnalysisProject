method client(n: Int) returns (res: Int) 
    requires n >= 0
    ensures res == n * (n + 1) / 2
{
    var i: Int := 0
    res := 0
    while (i < n) 
        invariant i <= n
        invariant res == i * (i + 1) / 2
    {
        i := i + 1
        res := res + i
    }
    assert i ==  n
}